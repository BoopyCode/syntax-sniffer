# Syntax Sniffer

Detects code smells like trailing whitespace, TODOs, print statements, and other programming sins. Run: `python syntax_sniffer.py <file_or_directory>` to find out how bad your code really is.